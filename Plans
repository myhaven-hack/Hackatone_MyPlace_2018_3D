Bot Plan:
- If any of the quotas are met, send an SMS to the family.
- Receive the message and parse respond
- If 'STOP', then shut down plug manually
- Otherwise, keep collecting data

WebApp Plan:
- Create JSON file with everything I've collected
- Create a standard code for Off/On device
- If on, then show off status and give all data.
- Otherwise, show off status
- Keep above

Script Plan
- Figure out how to construct dataframes
- Format API datetime output for data frame and graphs
- Restart count after a day, a week, and a month